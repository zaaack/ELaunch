module.exports = {
  plugins: {
    // app: {
    //   path: `${__dirname}/../plugins/app/index.js`,
    //   default: true,
    //   config: {}
    // },
    find: {
      // path: `${__dirname}/../plugins/find/index.js`,
      config: {
      }
    }
  }
}
