var fs = require('fs-extra')
var path = require('path');

let esDir = `${config.dataPath}/app/everything`,
etPath = `${esDir}/Everything.exe`


module.exports = {
  ensureEverything(config){

    if(config.dataPath)
  }
}
